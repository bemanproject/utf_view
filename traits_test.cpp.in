module;

#include <iterator>
#include <type_traits>

module p2728:traits_test;

import std_archetypes;
import :traits;

namespace p2728 {

  static_assert(
    std::is_same_v<
      std::input_iterator_tag,
      @*bidirectional-at-most-t*@<std_archetypes::input_iterator_archetype>>);
  static_assert(
    std::is_same_v<
      std::forward_iterator_tag,
      @*bidirectional-at-most-t*@<std_archetypes::forward_iterator_archetype>>);

} // namespace p2728
